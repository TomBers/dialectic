<!DOCTYPE html>
<html lang="en" class="[scrollbar-gutter:stable]">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <meta
      name="description"
      content={
        assigns[:page_description] ||
          "Turn messy linear chat into visual maps. Explore complex topics, reason together, and build a living knowledge base with MuDG."
      }
    />
    <%= if assigns[:noindex] do %>
      <meta name="robots" content="noindex, nofollow" />
    <% end %>
    <link
      rel="canonical"
      href={assigns[:canonical_url] || DialecticWeb.Endpoint.url() <> (@conn.request_path || "")}
    />
    <meta property="og:url" content={DialecticWeb.Endpoint.url() <> (@conn.request_path || "")} />
    <meta property="og:site_name" content="MuDG" />
    <meta property="og:type" content={assigns[:og_type] || "website"} />
    <meta
      property="og:title"
      content={assigns[:page_title] || "MuDG - Collaborative AI-Augmented Learning"}
    />
    <meta
      property="og:description"
      content={
        assigns[:page_description] ||
          "Turn messy linear chat into visual maps. Explore complex topics, reason together, and build a living knowledge base with MuDG."
      }
    />
    <%= if assigns[:og_image] do %>
      <meta property="og:image" content={@og_image} />
      <meta name="twitter:image" content={@og_image} />
    <% end %>
    <meta name="twitter:card" content="summary_large_image" />
    <meta
      name="twitter:title"
      content={assigns[:page_title] || "MuDG - Collaborative AI-Augmented Learning"}
    />
    <meta
      name="twitter:description"
      content={
        assigns[:page_description] ||
          "Turn messy linear chat into visual maps. Explore complex topics, reason together, and build a living knowledge base with MuDG."
      }
    />

    <%= if assigns[:json_ld] do %>
      <script type="application/ld+json">
        {raw(@json_ld)}
      </script>
    <% end %>

    <.live_title default="MuDG" suffix=" : Explore Ideas Together">
      {assigns[:page_title]}
    </.live_title>
    <link rel="icon" type="image/svg+xml" href={~p"/images/favicon.svg"} />
    <link phx-track-static rel="stylesheet" href={~p"/assets/app.css"} />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/app.js"}>
    </script>
    <link rel="preconnect" href="https://rsms.me/" />
    <link rel="stylesheet" href="https://rsms.me/inter/inter.css" />
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-NZDE9PL5FG">
    </script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'G-NZDE9PL5FG');
    </script>
  </head>
  <body class="bg-white">
    <% active_path = @conn.request_path %>
    <header
      class="sticky top-0 flex items-center h-12 bg-gradient-to-r from-[#3a0ca3]/90 to-[#4361ee]/90 backdrop-blur-md shadow-sm z-[9999]"
      id="userHeader"
    >
      <nav class="flex items-center justify-between px-4 w-full text-sm">
        <div class="flex items-center gap-4">
          <.link
            href={~p"/"}
            class="flex items-center gap-2 text-white hover:text-white/90 transition"
          >
            <img src={~p"/images/favicon.svg"} alt="MuDG" class="h-6 w-6" />
            <span class="font-bold tracking-tight hidden sm:inline">MuDG</span>
          </.link>
        </div>
        <div class="flex items-center gap-1 sm:gap-2">
          <.link
            href={~p"/intro/how"}
            class={[
              "inline-flex items-center gap-1.5 rounded-lg px-2.5 py-1.5 text-sm font-medium transition",
              if(active_path == "/intro/how",
                do: "bg-white/20 text-white",
                else: "text-white/70 hover:text-white hover:bg-white/10"
              )
            ]}
          >
            <.icon name="hero-book-open" class="h-4 w-4" />
            <span class="hidden sm:inline">Guide</span>
          </.link>
          <%= if @current_user do %>
            <.link
              href={~p"/my/ideas"}
              class={[
                "inline-flex items-center gap-1.5 rounded-lg px-2.5 py-1.5 text-sm font-medium transition",
                if(active_path == "/my/ideas",
                  do: "bg-white/20 text-white",
                  else: "text-white/70 hover:text-white hover:bg-white/10"
                )
              ]}
            >
              <.icon name="hero-light-bulb" class="h-4 w-4" />
              <span class="hidden sm:inline">My Ideas</span>
            </.link>
            <.link
              href={~p"/"}
              class="inline-flex items-center gap-1.5 rounded-lg bg-white/15 px-3 py-1.5 text-sm font-semibold text-white ring-1 ring-white/20 hover:bg-white/25 transition"
            >
              <.icon name="hero-plus" class="h-4 w-4" />
              <span class="hidden sm:inline">New</span>
            </.link>
            <.link
              href={~p"/users/settings"}
              class={[
                "inline-flex items-center gap-1.5 rounded-lg px-2.5 py-1.5 text-sm font-medium transition",
                if(active_path == "/users/settings",
                  do: "bg-white/20 text-white",
                  else: "text-white/70 hover:text-white hover:bg-white/10"
                )
              ]}
            >
              <.icon name="hero-cog-6-tooth" class="h-4 w-4" />
              <span class="hidden sm:inline">{@current_user.email}</span>
            </.link>
          <% else %>
            <.link
              href={~p"/users/register"}
              class="inline-flex items-center gap-1.5 rounded-lg bg-white/15 px-3 py-1.5 text-sm font-semibold text-white ring-1 ring-white/20 hover:bg-white/25 transition"
            >
              Register
            </.link>
            <.link
              href={~p"/users/log_in"}
              class="inline-flex items-center gap-1.5 rounded-lg px-2.5 py-1.5 text-sm font-medium text-white/70 hover:text-white hover:bg-white/10 transition"
            >
              Log in
            </.link>
          <% end %>
        </div>
      </nav>
    </header>

    <main>
      {@inner_content}
    </main>
  </body>
</html>
