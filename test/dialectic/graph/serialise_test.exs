defmodule Dialectic.Graph.SerialiseTest do
  use ExUnit.Case, async: true

  @moduledoc false

  test "placeholder: serialization will be covered via graph_to_json/Vertex formatting in a future update" do
    assert true
  end
end
