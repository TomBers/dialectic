defmodule DialecticWeb.PageControllerTest do
  use DialecticWeb.ConnCase

  # test "GET /", %{conn: conn} do
  #   conn = get(conn, ~p"/")
  #   assert html_response(conn, 200) =~ "<h1>Graph demo</h1>"
  # end
end
